 let multiplicando = 0;
 let limite= 0;
 let produto=0;
 let contador=1;
 console.log ("inicio do programa")
 multiplicando= parseInt(prompt("a tabuada de qual numero você deseja montar"))
 limite + parseInt(prompt(" até que número a tabuada irá"))

 while ( contador<=limite  )
 produto= multiplicando+ contador;
 console.log (`${multiplicando}) *${contador} = ${produto}`);
  contador ++;
 console.log (" fim do programa ")